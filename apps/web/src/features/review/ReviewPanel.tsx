export interface ReviewHunkView { id: string; path: string; before: string; after: string; eventSeq?: number; decision?: "accepted" | "rejected"; }
export interface ReviewPanelProps { hunks: readonly ReviewHunkView[]; onDecision: (hunk: ReviewHunkView, decision: "accepted" | "rejected", feedback?: string) => void; onSeek: (seq: number) => void; }
export function ReviewPanel({ hunks, onDecision, onSeek }: ReviewPanelProps) { return <section aria-label="Review changes" className="settings-panel"><h2>Review changes</h2>{hunks.map((hunk) => <article key={hunk.id}><button onClick={() => hunk.eventSeq === undefined ? undefined : onSeek(hunk.eventSeq)}><strong>{hunk.path}</strong></button><pre>{`- ${hunk.before}\n+ ${hunk.after}`}</pre><button disabled={hunk.decision !== undefined} onClick={() => onDecision(hunk, "accepted")}>Accept</button><button disabled={hunk.decision !== undefined} onClick={() => onDecision(hunk, "rejected")}>Reject</button><span>{hunk.decision ?? "pending"}</span></article>)}</section>; }
